# Sync upstream — 2026-05-08

## Rapport final

| Branche | SHA avant | SHA après | Commits intégrés |
|---|---|---|---|
| `main` | `ce4c782fd` | `59c35100c` | 96 commits upstream (jusqu'au tag `nightly`) |
| `eurelis/main` | `48fc95d6f` | `3275062ca` | 5 commits Eurelis rebasés |

## Cible

Tag upstream : `nightly` (`59c35100c56ab8954f1c4066cac512eb51bbf194`)

## Conflits résolus

### `uv.lock` — commit `cb70f7362` (feat(eurelis/deps): add Eurelis dependencies)

**Cause** : `uv.lock` modifié en amont dans `nightly` (nouvelles dépendances upstream) et par le commit Eurelis (ajout de `zhipuai>=2.0.1`).

**Résolution** : acceptation de la version upstream (`git checkout --theirs uv.lock`) puis régénération complète via `uv lock`. La dépendance `zhipuai` est correctement présente dans `pyproject.toml` et a été intégrée au lock file régénéré.
