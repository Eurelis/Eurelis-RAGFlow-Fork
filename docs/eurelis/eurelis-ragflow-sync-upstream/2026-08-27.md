# Sync upstream — 2026-08-27

Cible de synchronisation : **v0.27.0** (`ec9c08d80`).

## Rapport de synchronisation

| Branche | SHA avant | SHA après | Commits intégrés |
|---|---|---|---|
| `main` | `cb93883f3f8c975eecb2fed81210effeb3bdb06f` | `ec9c08d809f63ba2815090182fa225899d2437d5` | 1225 commits upstream |
| `eurelis/main` | `2524176aaa8f3653e6484fb37b4ca1ea62d0235d` | `e3511f0fdc4d2278dcf0241f7618c13f96f3fa32` | 66 commits Eurelis rebasés |

Tags de rollback : `backup/main-before-sync`, `backup/eurelis-main-before-sync-2026-08-26-v0.27.0`.

## Analyse des changements upstream

Répartition approximative des 1225 commits : ~741 fixes, ~143 features, ~90 commits Go (providers/CLI), ~42 docs/tests.

### Bugs corrigés notables

- **Migration `zhipuai` → `zai-sdk`** (`6a85d1c5c`) pour débloquer un correctif CVE pyjwt — notre assouplissement de contrainte `zhipuai>=2.0.1` est devenu sans objet (abandonné au rebase).
- **`_StreamSanitizer` / `_sanitize_answer`** dans `rag/llm/chat_model.py` : filtrage des tokens de contrôle MiniMax qui fuient dans le contenu — composé avec notre PII-unmasking (voir conflits).
- **HTML-escape du `empty_response`** dans `dialog_service` (DOMPurify supprimait les tags inconnus, UI bloquée sur « Searching… ») — composé avec notre payload `usage`.
- **`mindmap_task_finish_at`** : type corrigé `CharField` → `DateTimeField` en migration.
- **Pagination admin user detail** (`1127ce0cb`) — sans objet pour nous, la page est remplacée par les pages team Eurelis.
- Correctifs Markdown vide, whitelist `.bmp`, crash recherche `[+-]`, etc.

### Nouvelles fonctionnalités

- **Catalogue providers v0.27.0** (`0ae5961e1`) et montée de version générale.
- **Nouveaux doc engines** : GaussDB, SeekDB, SereneDB (variables `docker/.env`).
- **Refactor extractor « 5-in-1 »** (`2a1a93ed3`) : schéma modulaire unifié dataset/pipeline.
- **Wiki / skill / structure tasks** : nombreuses colonnes `knowledgebase` (renommage `artifact_*` → `wiki_*`, `skill_task_id`, colonnes `structure_*`).
- **`rag_agent`** remplace `async_chat` dans les endpoints chat (rename API interne).
- **Schéma zod du Search déplacé** vers `web/src/pages/next-search/search-setting-hooks.ts` (`useSearchSettingFormSchema`).
- **`CLAUDE.md` upstream devenu symlink vers `AGENTS.md`** — le fork conserve son `CLAUDE.md` réel (fichier normal).
- **`ENABLE_REGISTER`** : nouvelle variable lue par le serveur Go (le Python lit toujours `REGISTER_ENABLED`) — les deux forcées à `0` dans le fork.

### Providers Go ajoutés

~90 commits `Go:` (providers de modèles, CLI, build/release) — impact Eurelis nul.

### Fichiers en intersection (upstream ∩ Eurelis)

56 fichiers touchés des deux côtés. Points chauds ayant réellement conflicté (résolutions ci-dessous) ; le reste a auto-mergé (zones distinctes) :

| Zone | Fichiers | Verdict |
|---|---|---|
| Chat API + services | `chat_api.py`, `conversation_service.py`, `dialog_service.py` | **Risque de conflit récurrent** — rename `rag_agent`, usage-stats et group_work s'entrelacent |
| LLM runtime | `rag/llm/chat_model.py` | **Risque récurrent** — PII-unmasking et sanitizer upstream vivent au même endroit |
| Deps | `pyproject.toml`, `uv.lock` | **Risque récurrent** — pins litellm/deps Eurelis vs bumps upstream |
| Search settings web | `search-setting.tsx`, `search-setting-hooks.ts` | À surveiller — le champ `permission` Eurelis vit maintenant dans le schéma du hook upstream |
| Admin server | `admin/server/*` | Zones distinctes (imports uniquement) |
| Migrations | `api/db/db_models.py` | Zones distinctes — colonnes `permission` Eurelis ajoutées en fin de bloc |
| Autres (locales, routes, tailwind, task executor…) | — | Auto-merge propre |

### Commits Eurelis absorbés par upstream

- `9f2f4d285` — *fix(eurelis): admin password update crashes for SSO users* : écarté automatiquement (déjà présent upstream).
- `581f60b00` — *feat(eurelis): add BedrockRerank connector* : la contribution a été mergée upstream, qui embarque une version évoluée (limites `_MAX_DOC_CHARS`/`_MAX_SOURCES`) — commit devenu vide, supprimé de l'historique.
- Partiellement absorbé : `fc662e26f` (spaCy `fr_core_news_sm`) — l'upstream embarque désormais lui-même les modèles spaCy fr/es/pt/ja/de/zh dans `pyproject.toml`.

## Conflits résolus

8 arrêts de rebase, tous résolus manuellement :

1. **`896f2c4f0` (deps zhipuai)** — `pyproject.toml` : upstream a remplacé `zhipuai` par `zai-sdk` (CVE pyjwt) → version upstream conservée, ligne `zhipuai` abandonnée (validé utilisateur).
2. **`f1581e2e2` (delete chat blobs)** — `chat_api.py` : union d'imports (`rag_agent` upstream + `FileService` Eurelis).
3. **`c52c701a3` (group_work team management)** — 6 fichiers :
   - `chat_api.py` : union imports ; blocs Eurelis `_ensure_owned_chat` (accès team) et filtrage sessions non-propriétaire conservés avec la condition upstream `store_history_messages`.
   - `db_models.py` : bloc migrations upstream complet + 2 colonnes `permission` Eurelis.
   - `search_service.py` : version Eurelis (filtre `permission == TEAM`).
   - `search-setting.tsx` : structure upstream (schéma déplacé) + champ `permission` réinjecté dans `search-setting-hooks.ts`.
   - `user-detail.tsx` : suppression Eurelis maintenue (page remplacée par les pages team).
   - `admin/server/services.py` : union d'imports.
4. **`e90b47449` (pii-masking)** — `rag/llm/chat_model.py` : composition sanitizer upstream → PII-unmasking Eurelis (non-stream : `_sanitize_answer` puis `unmask_text` ; stream : `_sanitizer.feed` chaîné dans `_pii_unmasker.process_chunk`, y compris le flush). `docker/.env` : `REGISTER_ENABLED=0` + `ENABLE_REGISTER=0`.
5. **`3755016b9` (usage-stats)** — 10 fichiers : unions d'imports ; `rag_agent` substitué à `async_chat` dans les boucles instrumentées ; `stats()` version Eurelis (`current_user.id`) cohérente avec ses imports ; `API4ConversationService.stats` version Eurelis (join `UserCanvas` pour `source="agent"`) ; journalisation ingestion re-encapsulée en try/except aux côtés du `increment_chunk_num` durci upstream ; `build_chunks(binary, on_chunking_start)` + `start_ingestion_llm_tracking()` ; `CLAUDE.md` : fichier réel Eurelis conservé face au symlink upstream.
6. **`1729a73db` (joined-team chats)** — `chat_api.py` : `joined_tenant_ids` réinjecté dans l'appel reformaté upstream.
7. **`942b42a64` (healthz litellm)**, **`056cbbd38` (team AI summary)**, **`68489eb6d` (AILAB-22)** — extensions Eurelis conservées sur les formes upstream (`resolve_model_config(owner_tenant_id, …)` ; `max_completion_tokens` toujours nécessaire car `_clean_conf` upstream supprime encore `max_tokens` ; condition température upstream `>= 0` reprise).
8. **`93b5e798e` (litellm bump)** — pin Eurelis `litellm==1.96.2` conservé (> minimum CVE upstream 1.84.0).

### Incident et réparation d'historique

Aux stops 4 et 5 (pii-masking, sitemap), des conflits dans `docker/.env`, `pyproject.toml`, `common/data_source/__init__.py` et `uv.lock` ont été stagés avec leurs marqueurs (sortie de conflit tronquée + `git add -A`), polluant ~56 commits intermédiaires. **Réparation effectuée avant push** par rebase scripté (`edit` sur les 3 commits concernés) : marqueurs corrigés dans leurs commits d'origine, `uv.lock` régénéré par `uv lock` au commit litellm-bump (presidio, trafilatura, litellm 1.96.2 intégrés). Vérification finale : aucun marqueur de conflit dans aucun des 66 commits ni dans l'arbre final.

`uv.lock` note : les commits antérieurs au bump litellm portent le lock upstream v0.27.0 nu (sans presidio/trafilatura) ; le lock complet est porté par `feat(eurelis/deps): bump litellm…`.
