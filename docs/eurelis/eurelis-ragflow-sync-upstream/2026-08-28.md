# Sync upstream — 2026-08-28

Cible de synchronisation : **v0.27.1** (`b9df87c4c`).

## Rapport de synchronisation

| Branche | SHA avant | SHA après | Commits intégrés |
|---|---|---|---|
| `main` | `ec9c08d809f63ba2815090182fa225899d2437d5` | `b9df87c4c75a5b0d35c90d15329fc0f6f91cb73e` | 333 commits upstream |
| `eurelis/main` | `9dbca9a0c0674e227317884c72b76d79e3d046e8` | `5eba3ba0c8576b09b00d449d777bb9e8b8f27baf` | 68 commits Eurelis rebasés |

Tags de rollback : `backup/main-before-sync`, `backup/eurelis-main-before-sync-2026-08-28-v0.27.1`.

## Analyse des changements upstream

Répartition des 333 commits : ~245 fixes, ~38 features, ~7 refactors, 3 Go.

### Bugs corrigés notables
- **`rag_agent` ne forwarde plus les clés de bookkeeping des messages au provider** (#18663) — zone chaude chat Eurelis.
- **Le prompt système du dialog est honoré en chat « reasoning »** (#18842).
- **Retrieval SQL : tenant dérivé du propriétaire du dataset**, pas du tenant du chat (#18518) — même philosophie que notre découplage owner/acting.
- **Rerank : `prefetch_size` → `rerank_candidates`** (#18718, #18300) — nouveau champ dans les schémas de formulaires chat/search (absorbé dans nos résolutions).

### Nouvelles fonctionnalités
- **Bedrock : authentification par API key** (#18301) — à surveiller vis-à-vis du protocole de clé JSON Eurelis (`auth_mode`/`bedrock_*`).
- **`resolve_llm_setting` pour la synthèse Search** — le résumé IA utilise la config LLM de l'app au lieu d'un `temperature: 0.1` codé en dur (composé avec notre chronométrage usage_log).
- Connecteurs data source : Azure DevOps, Xquik, You.com (agent), Box/S3/OCI/Azure Blob côté Go-syncer ; registre central `CONNECTOR_BY_SOURCE` + `build_connector_for_source`.
- Provider Synthorai ; timeout MCP configurable ; extraction d'images XLSX ; extractor multi-tâches.

### Providers Go ajoutés
3 commits Go (connecteurs syncer) — impact Eurelis nul.

### Fichiers en intersection (upstream ∩ Eurelis)
38 fichiers. Points chauds confirmés (conflits réels ci-dessous) : schémas de formulaires web chat/search (`use-chat-setting-schema.tsx`, `search-setting.tsx`), registre des data sources (`common/data_source/`), `dialog_service.py`. Le reste a auto-mergé (zones distinctes), notamment `chat_api.py`, `chat_model.py`, `db_models.py`, `pyproject.toml`/`uv.lock` (seul le numéro de version upstream a bougé — le lock Eurelis avec litellm 1.96.2 est conservé tel quel).

### Commits Eurelis absorbés par upstream
Aucun sur cette passe.

## Conflits résolus

3 arrêts de rebase, tous résolus manuellement :

1. **`4910dbf5e` (group_work)** — `use-chat-setting-schema.tsx` : schéma upstream enrichi (`rerankCandidatesCountSchema`, `superRefine`/`chatPromptKbIssues`) conservé + champ Eurelis `permission: z.enum(['me','team'])` réinjecté. `search-setting.tsx` : union d'imports (`RerankCandidatesCountFormField` upstream + `PermissionFormField` Eurelis) ; import `ModelTreeSelect` devenu inutilisé, abandonné.
2. **`5c9fedecd` (sitemap)** — `config.py` : union des enums (`AZURE_DEVOPS` upstream + `SITEMAP` Eurelis). `__init__.py` : `__all__` upstream trié + `SitemapConnector` en ordre alphabétique ; **adaptation nécessaire** : entrée `FileSource.SITEMAP: SitemapConnector` ajoutée au nouveau registre upstream `CONNECTOR_BY_SOURCE` (sans elle, l'endpoint de validation `connector_api.py` rejetterait le type sitemap). `index.tsx` (web) : union d'imports (`Search` upstream + `t`/`Globe` Eurelis).
3. **`c3603e92e` (usage-stats)** — `dialog_service.py` : union d'imports (`resolve_llm_setting` upstream + `eurelis_usage_log`) ; dans `async_ask`, `gen_conf` upstream (résolu depuis `search_config.llm_setting`) composé avec le chronométrage Eurelis `ask_start_ts`.

Vérifications post-rebase : aucun marqueur de conflit dans aucun des 68 commits ni dans l'arbre ; `litellm==1.96.2` conservé dans `uv.lock` ; syntaxe Python validée sur les fichiers résolus.
